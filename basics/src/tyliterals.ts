let myName = "john"
// by default typescript will infer the above type as string because it's value is a string
const myNameOne = "Bob"
// but here typescript will infer the type to a specific value of the variable and this is called type literals
// hence you can't resign the const
